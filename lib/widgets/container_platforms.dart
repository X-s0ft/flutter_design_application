import 'package:flutter/material.dart';

class ContainerPlatformWidget extends StatelessWidget {
  const ContainerPlatformWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}