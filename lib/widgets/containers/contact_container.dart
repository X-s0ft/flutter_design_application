import 'package:flutter/material.dart';

class ContactTainer extends StatelessWidget {
  const ContactTainer({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
